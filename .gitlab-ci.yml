---
image:
  name: golang

stages:
  - build

build:
  stage: build
  script:
    - printenv
    - pwd
    - ls
    - mkdir -p /go/src/github.com/individuwill
    - ln -s ./ /go/src/github.com/individuwill/mcast
    - ls
    - go build
